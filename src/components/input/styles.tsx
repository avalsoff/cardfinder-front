import styled from 'styled-components'

export const Input = styled.input`
  font-size: 18px;
  line-height: 24px;
  padding: 14px 16px;
  border: 1px solid #cacaca;
  border-radius: 12px;
  outline: none;

  &::placeholder {
    color: #cacaca;
  }
`
